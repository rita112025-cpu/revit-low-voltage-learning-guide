# 測試紀錄（2026-09-13）

## 基底確認與差異

工作目錄起初沒有 `.git`，因此 `git status` 顯示「not a git repository」，也無可保存的既有未提交改動。未找到適用的 `AGENTS.md`。原檔具有 `panel-unit-01`～`07`、`UNIT_TITLES`、`UNIT_ORDER`、`HEIGHT_LIMITS`、28 個 `data-check-id`、`beforeprint`／`afterprint`，確認為指定的 Claude 架構。附帶 ZIP 的 `index.html` 僅約 9 KB，與原工作檔約 49 KB 不同，未拿它覆蓋基底。

| 檔案 | 開始前 SHA-256 | 完成後 SHA-256 |
| --- | --- | --- |
| `index.html` | `DDCB71981020BDCA97745B130CBC084B61597F18642A65226A851D7AF3B22F1E` | `A74102EA2C7677BCE9308865AC601091D3B35AB038D83F9EB154372185CF7FB5` |
| `styles.css` | `A3A5216FB9D14943F0627D20A39AFA5C312833C0FC00A592B21832333F7D8BB4` | `96C588E46F94DD4767CBE49F193843153B409254E55C321D49D85C999711DDF8` |
| `app.js` | `898BA49CB59876473B3643B49A37BE479C30655510291E584848DDD3E2062B57` | `DFA399982277D6307C005CA2637062875596C3A629FB3BC10E6A8DB61EB9524B` |

開始前未建立原檔副本，且未找到對應的 Claude 附件檔；因此無法提供逐行 unified diff。以上雜湊比較、原架構識別與 `CHANGELOG.md` 記錄可確認實際改動，這是本次差異稽核的限制。

## 語法與原始碼檢查

- `node --check app.js` 通過。
- 原始碼與瀏覽器 DOM 檢查確認七單元、28 個原有驗收 ID 唯一，CSS／JS 使用相對路徑，未引入 React、後端或第二套列印教材。
- Autodesk 官方頁面逐一開啟核對：IFC 連結、定位選項、CAD 單位與「僅目前視圖」、Mark 標籤、共用參數、共用巢狀構件與教學來源。原 `help.autodesk.com/view/RVT/2025/...` 連結回傳 Page Not Found，已改成可開啟的 2022 官方頁。查核僅使用通用技術詞，未傳送附件內容。

## 瀏覽器實測

使用本機已安裝的 Python Playwright／Chromium 151，在 `file://` 開啟正式 `index.html`，以暫時性 Playwright 腳本操作正式 CSS／JS；未為網頁安裝測試框架。結果：

- 七單元導航、手機選單收合與單元標題焦點通過；28 個 ID 無重複。
- 舊 key 的合法進度重整保留；勾選更新導航與總表；重設取消維持原值、確認後清空。
- 空資料、有效物件、`null`、`[]`、損壞 JSON、未知 ID、非 boolean 值皆通過。非法值清理後有效項目仍保留，頁面有持續可見提示。模擬 `getItem`、`setItem` 丟錯時仍可勾選並提示。
- 高度輸入 2700／800→2300／3100、5000／800→4600／5400、1000／3000→-500／2500、0／1、6000／3000 均通過。空白、文字、負值與超限有具體錯誤；有效改無效後，切單元、勾選、列印皆無舊結果或設備圖。連續修改以最後一次輸入為準。
- UTF-8 TXT 下載內容含當前 2／28 進度、逐項狀態及無效高度提示。成果總表與勾選一致。
- 列印按鈕呼叫列印；列印媒體下七單元均顯示，FAQ 展開後可還原，黑白勾選含「已完成」文字。由不同單元觸發的 Chromium A4 PDF 成功產生；明確進入 print media 並展開 FAQ 的 PDF 有 16 頁。無效高度時無舊設備圖。
- 360、390、1280px 無整頁橫向溢出；360px 的 200% CSS zoom 仍可見標題與勾選、無橫向溢出。主要步驟／提示／驗收文字 computed size 為 18px，輔助標籤 14px。已人工檢視手機高度工具截圖，並修正無效輸入時的過大空白。
- 模擬巢狀子目錄開啟時，正式相對 CSS／JS 仍載入。Playwright 無未處理頁面錯誤。

## 尚未驗證

- 未在實體手機、各瀏覽器與 GitHub Pages 實際站點驗證；子目錄測試為本機模擬。
- 已生成 PDF 並檢查頁數與列印 DOM，但未逐頁人工檢視 PDF 實際文字與裁切，也未操作系統的 Ctrl+P 視窗。
- 沒有 Revit 安裝環境，未做 Revit 實機放置、明細表或出圖測試；專案座標、族模式、實際版本與圖框仍需由使用者在其環境核對。
